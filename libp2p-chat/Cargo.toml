[package]
name = "libp2p-chat"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.50.0", feature = ["full"] }
async-trait = "0.1.89"
futures = "0.3.32"
libp2p = { version = "0.56.0", features = ["tokio", "gossipsub", "mdns", "noise", "macros", "tcp", ] }
